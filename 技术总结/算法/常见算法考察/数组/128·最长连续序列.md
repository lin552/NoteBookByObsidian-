---
创建时间: 2025-06-12 19:23:14
作者: wangxiaoming
tags:
  - 哈希表
---
#### 一、题目

给定一个未排序的整数数组 `nums` ，找出数字连续的最长序列（不要求序列元素在原数组中连续）的长度。

请你设计并实现时间复杂度为 `O(n)` 的算法解决此问题。

**示例 1：**

**输入：**`nums` = [100,4,200,1,3,2]
**输出：**4
**解释：**最长数字连续序列是 [1, 2, 3, 4]。它的长度为 4。

**示例 2：**

**输入：**`nums` = [0,3,7,2,5,8,4,6,0,1]
**输出：**9

**示例 3：**

**输入：**`nums` = [1,0,1,2]
**输出：**3

**提示：**

- `0 <= nums.length <= 105`
- `-109 <= nums[i] <= 109`

#### 二、解题思路
1.哈希表：哈希表去重存数组元素，遍历Set，寻找不连续的最小，每个连续子集找最小就行，然后while循环判断最长连续的个数
用Math.max()从多个连续集中找最大的。



#### 三、代码
```java
//1.哈希表
class Solution {
    public int longestConsecutive(int[] nums) {
    //遍历数组加入到Set中，hashset无序
        Set<Integer> num_set = new HashSet<Integer>();
        for(int num:nums){
            num_set.add(num);
        }
        //记录最长连续数
        int longestStreak = 0;
        //遍历Set
        for(int num:num_set){
        //如果set中没有递减的说明就是当前连续子集最小元素
            if(!num_set.contains(num - 1)){
                int currentNum = num;
                int currentStreak = 1;
                //循环查最长连续的个数
                while(num_set.contains(currentNum + 1)){
                    currentNum += 1;
                    currentStreak += 1;
                }
                longestStreak = Math.max(longestStreak,currentStreak);
            }
        }
        return longestStreak;
    }
}
```